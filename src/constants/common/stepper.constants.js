export const status = Object.freeze({
  WAITING: 'waiting',
  CURRENT: 'current',
  COMPLETED: 'completed',
});

export const orientationType = Object.freeze({
  VERTICAL: 'vertical',
  HORIZONTAL: 'horizontal',
});

export default {
  status,
  orientationType,
};
